// Relying Party ID - typically the domain name
export const rpId = 'localhost';

// Relying Party Name
export const rpName = 'Altered TCG';

// Expected origin for WebAuthn operations
export const expectedOrigin = 'http://localhost:5173';
